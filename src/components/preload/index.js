import React, { Fragment } from 'react';
import PreLoad from './preload'
const Index = () => {
    return (
        <Fragment>
            <PreLoad />
        </Fragment>
    );
}

export default Index;
